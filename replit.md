# RNTX Admin Panel on Replit

## Run

- Workflow: `Start application`
- Command: `PORT=5000 npm start`
- Preview port: `5000`

## Required Secrets

- `ADMIN_USERNAME`
- `ADMIN_PASSWORD`
- `SESSION_SECRET`

The SQLite database is stored in `rntx.db` and is intentionally excluded from Git.